# Loose pins on purpose. Tested with verifiers 0.1.14 + Python 3.13.
# If pip says "No matching distribution found" the most common cause is a
# Python version with no wheels yet — try Python 3.13.
fastapi
uvicorn[standard]
sse-starlette
pydantic
openai
verifiers
datasets
pyyaml
